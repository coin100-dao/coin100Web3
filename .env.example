# Network Configuration
ALCHEMY_API_KEY=your_alchemy_api_key
PRIVATE_KEY=your_wallet_private_key

# Contract Deployment Parameters
COIN100_ADDRESS=0x0000000000000000000000000000000000000000
PAYMENT_TOKEN=0x0000000000000000000000000000000000000000  # Use zero address for native token (ETH)
RATE=0.001  # Exchange rate (e.g., 0.001 means 0.001 ETH per C100)
TOKEN_SYMBOL=ETH
TOKEN_NAME="Ethereum"
TOKEN_DECIMALS=18

# Optional: Public Sale Timing (Unix timestamps)
START_TIME=  # Optional: Leave empty for default (1 minute after deployment)
END_TIME=    # Optional: Leave empty for default (30 days after start) 